#include <stdio.h>
#include <bits//stdc++.h>

using namespace std;

int main(){
    int n, k, aproximado, resultado;
    cin >> n >> k;
    aproximado = (n/k)+1;
    resultado = k*aproximado;

    cout << resultado;
    
}